import styles from './Nav.module.css';

const Nav = () => {
  return (
    <header className={styles.container}>
      <nav>
        <p>Hello world</p>
      </nav>
    </header>
  );
};

export default Nav;
